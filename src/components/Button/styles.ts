import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    gap: 8,
    elevation: 2,
    borderRadius: 12,
    paddingVertical: 12,
    flexDirection: 'row',
    paddingHorizontal: 18,
  },
});
